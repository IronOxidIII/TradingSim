<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="TradingSim"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".feature.portfolio.PortfolioActivity"
            android:label="Портфель" />

        <activity
            android:name=".feature.trading.ui.TradingActivity"
            android:label="Торговля" />

        <activity
            android:name=".feature.history.HistoryActivity"
            android:label="История операций" />

        <activity
            android:name=".feature.main.MainActivity"
            android:exported="true"
            android:label="TradingSim">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>